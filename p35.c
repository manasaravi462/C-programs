#include<stdio.h>
int main()
{
    int a=7,b=2,add,sub,mul,div;
    add=a+b;
    sub=a-b;
    mul=a*b;
    div=a/b;
    printf("Addition is %d\nsubraction is %d\nmultiplication is %d\ndivison is %d",add,sub,mul,div);
    return 0;
}
